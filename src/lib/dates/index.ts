/**
 * Dates Domain — date utilities and term schedule helpers.
 *
 * Re-exports from the parent-level utility files so consumers can import from
 * `@/lib/dates` as a cohesive module.
 */

export * from '../dateUtils'
export * from '../termSchedule'
