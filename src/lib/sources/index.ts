/**
 * Sources Domain — funding sources (debit, credit, cash, wallets), linked
 * accounts, source balances, and general account utilities.
 *
 * Re-exports from the parent-level utility files so consumers can import from
 * `@/lib/sources` as a cohesive module.
 */

export * from '../fundingSources'
export * from '../sourceBalances'
export * from '../linkedAccounts'
export * from '../accountUtils'
